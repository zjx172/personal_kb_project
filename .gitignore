# OpenAPI 生成文件
openapi.json

# 前端依赖与可选的生成代码
apps/**/node_modules

# 后端虚拟环境/本地数据
apps/api/venv
apps/api/vector_store
apps/api/kb.db
apps/api/.env
apps/api/uploads

# 评测后端虚拟环境（如有）
apps/eval-api/venv

# macOS 与编辑器临时文件
.DS_Store
*.log
